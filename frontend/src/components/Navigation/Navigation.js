(function(){"use strict";class Navigation{constructor(){this.header=document.getElementById("mainHeader");this.toggle=document.getElementById("navToggle");this.navList=document.getElementById("navList");this.themeToggle=document.getElementById("themeToggle");this.moonIcon=document.getElementById("moonIcon");this.sunIcon=document.getElementById("sunIcon");this.links=document.querySelectorAll(".nav__link");this.init()}init(){this.setupMobileMenu();this.setupScrollEffects();this.setupThemeToggle();this.setupActiveLink();this.setupBookButton()}setupMobileMenu(){if(!this.toggle||!this.navList)return;this.toggle.addEventListener("click",()=>{const isOpen=this.navList.classList.toggle("open");this.toggle.setAttribute("aria-expanded",isOpen);document.body.style.overflow=isOpen?"hidden":""});this.links.forEach(link=>{link.addEventListener("click",()=>{this.navList.classList.remove("open");this.toggle.setAttribute("aria-expanded","false");document.body.style.overflow=""})});window.addEventListener("resize",()=>{if(window.innerWidth>768){this.navList.classList.remove("open");this.toggle.setAttribute("aria-expanded","false");document.body.style.overflow=""}})}setupScrollEffects(){let lastScroll=0;window.addEventListener("scroll",()=>{const currentScroll=window.pageYOffset;if(currentScroll>50){this.header.classList.add("scrolled")}else{this.header.classList.remove("scrolled")}lastScroll=currentScroll})}setupThemeToggle(){if(!this.themeToggle)return;const savedTheme=localStorage.getItem("theme")||"dark";this.setTheme(savedTheme);this.themeToggle.addEventListener("click",()=>{const currentTheme=document.documentElement.getAttribute("data-theme");const newTheme=currentTheme==="dark"?"light":"dark";this.setTheme(newTheme)})}setTheme(theme){document.documentElement.setAttribute("data-theme",theme);localStorage.setItem("theme",theme);if(theme==="dark"){this.moonIcon.style.display="block";this.sunIcon.style.display="none"}else{this.moonIcon.style.display="none";this.sunIcon.style.display="block"}window.dispatchEvent(new CustomEvent("themeChanged",{detail:{theme}}))}setupActiveLink(){const sections=document.querySelectorAll("section[id]");window.addEventListener("scroll",()=>{let current="";const scrollPos=window.pageYOffset+100;sections.forEach(section=>{const sectionTop=section.offsetTop;const sectionHeight=section.offsetHeight;if(scrollPos>=sectionTop&&scrollPos<sectionTop+sectionHeight){current=section.getAttribute("id")}});this.links.forEach(link=>{link.classList.remove("active");if(link.getAttribute("href")==="#"+current){link.classList.add("active")}})})}setupBookButton(){const bookBtn=document.querySelector("[data-action=\"book\"]");if(bookBtn){bookBtn.addEventListener("click",()=>{alert("Booking system coming soon!")})}}}if(document.readyState==="loading"){document.addEventListener("DOMContentLoaded",()=>{new Navigation()})}else{new Navigation()}})();
